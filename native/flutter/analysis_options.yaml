include: package:flutter_lints/flutter.yaml
# Note: do NOT exclude lib/generated — excluding it removes its symbols from the
# analysis context, which makes JazerTheme/JazerColors/etc. "undefined" where imported.
# The generated file carries its own `// ignore_for_file: type=lint` instead.
