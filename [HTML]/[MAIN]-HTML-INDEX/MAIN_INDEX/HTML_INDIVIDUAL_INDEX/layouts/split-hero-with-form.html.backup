<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Split Hero with Form</title>
  <style>
    /* ==================== RESET ==================== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 0; /* Remove body padding for full width hero */
    }

    /* ==================== HERO SECTION ==================== */
    .split-hero {
      display: flex;
      flex-wrap: wrap; /* Allows sections to stack on small screens */
      width: 100%;
      min-height: 100vh; /* Ensure hero takes full viewport height */
    }

    .hero-content-left {
      flex: 1; /* Takes equal space */
      background: linear-gradient(135deg, #3498db 0%, #8e44ad 100%);
      color: #fff;
      padding: 3rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
      text-align: left;
    }

    .hero-content-left h1 {
      font-size: 3.5rem;
      margin-bottom: 1.5rem;
      line-height: 1.2;
    }

    .hero-content-left p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      max-width: 600px;
    }

    .hero-cta {
      background-color: #2ecc71;
      color: #fff;
      padding: 1rem 2rem;
      border-radius: 50px;
      text-decoration: none;
      font-weight: 600;
      font-size: 1.1rem;
      transition: background-color 0.2s;
    }

    .hero-cta:hover {
      background-color: #27ae60;
    }

    .hero-form-right {
      flex: 1; /* Takes equal space */
      background-color: #fff;
      padding: 3rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      box-shadow: -5px 0 20px rgba(0, 0, 0, 0.05);
    }

    .hero-form-right h2 {
      font-size: 2rem;
      color: #2c3e50;
      margin-bottom: 1.5rem;
      text-align: center;
    }

    /* ==================== FORM STYLES ==================== */
    .registration-form {
      width: 100%;
      max-width: 400px;
      text-align: left;
    }

    .form-group {
      margin-bottom: 1.2rem;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 600;
      color: #555;
    }

    .form-group input[type="text"],
    .form-group input[type="email"],
    .form-group input[type="password"] {
      width: 100%;
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
      transition: border-color 0.3s, box-shadow 0.3s;
    }

    .form-group input:focus {
      outline: none;
      border-color: #3498db;
      box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.2);
    }

    .submit-btn {
      width: 100%;
      background-color: #3498db;
      color: #fff;
      padding: 14px;
      border: none;
      border-radius: 8px;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.2s, transform 0.2s;
    }

    .submit-btn:hover {
      background-color: #2980b9;
      transform: translateY(-2px);
    }

    .form-footer {
      margin-top: 1.5rem;
      font-size: 0.9rem;
      color: #777;
      text-align: center;
    }

    .form-footer a {
      color: #3498db;
      text-decoration: none;
    }

    /* ==================== RESPONSIVE ==================== */
    @media (max-width: 900px) {
      .split-hero {
        flex-direction: column; /* Stack vertically */
        min-height: auto; /* Allow content to dictate height */
      }
      .hero-content-left,
      .hero-form-right {
        padding: 2.5rem 2rem;
        flex: none; /* Remove flex sizing */
        width: 100%;
      }
      .hero-content-left {
        text-align: center;
        align-items: center;
      }
      .hero-content-left h1 {
        font-size: 2.8rem;
      }
      .hero-content-left p {
        font-size: 1rem;
      }
      .hero-form-right {
        box-shadow: 0 -5px 20px rgba(0, 0, 0, 0.05); /* Adjust shadow for top */
      }
    }

    @media (max-width: 500px) {
      .hero-content-left h1 {
        font-size: 2.2rem;
      }
    }
  </style>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&family=Outfit:wght@400;600;700;800&family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../../jazer-brand.css">
  </head>
<body>

  <div class="split-hero">
    <div class="hero-content-left">
      <h1>Unlock Your Potential with Our Platform</h1>
      <p>Join thousands of professionals who are transforming their work with our intuitive tools and powerful features. Sign up today to get started!</p>
      <a href="#" class="hero-cta">Learn More</a>
    </div>

    <div class="hero-form-right">
      <h2>Create Your Account</h2>
      <form class="registration-form">
        <div class="form-group">
          <label for="fullName">Full Name</label>
          <input type="text" id="fullName" placeholder="John Doe" required>
        </div>
        <div class="form-group">
          <label for="email">Email Address</label>
          <input type="email" id="email" placeholder="john.doe@example.com" required>
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" placeholder="Min. 8 characters" required>
        </div>
        <button type="submit" class="submit-btn">Sign Up Free</button>
        <p class="form-footer">
          By signing up, you agree to our <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a>.
        </p>
      </form>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const form = document.querySelector('.registration-form');

      form.addEventListener('submit', (e) => {
        e.preventDefault(); // Prevent default form submission

        let isValid = true;
        const inputs = form.querySelectorAll('input[required]');

        inputs.forEach(input => {
          if (!input.value.trim()) {
            input.style.borderColor = 'red'; // Simple visual feedback for empty fields
            isValid = false;
          } else {
            input.style.borderColor = '#ccc';
          }
        });

        if (isValid) {
          alert('Form submitted successfully! (Simulated)');
          // In a real application, you would send the data to a server here.
          // e.g., fetch('/api/register', { method: 'POST', body: new FormData(form) });
          form.reset();
        } else {
          alert('Please fill in all required fields.');
        }
      });
    });
  </script>

</body>
</html>
